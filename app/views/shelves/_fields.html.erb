<%= form_for(@shelf) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

  <%= f.label :name %>
  <%= f.text_field :name, autofocus: true %>
  <br>

  <%= f.submit 'Save' %>
<% end %>