<% provide(:title, 'My Books') %>

<h1>Your Books</h1>

<ul>
  <% @books.each do |book| %>
  <li>
    <%= link_to book.title, book_path(book.id) %> |
    <%= link_to "delete", book, method: :delete, data: { confirm: 'Really?' } %>
    <% end %>
  </li>
</ul>